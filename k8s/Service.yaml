apiVersion: v1
kind: Service
metadata:
  name: k8s-alpha-svc
  labels:
    app: k8s-alpha
spec:
  type: LoadBalancer
  selector:
    app: k8s-alpha
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
